<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfunds.mapper.FxCustomerBalanceMapper">

    <resultMap id="BaseResultMap" type="com.xfunds.entity.FxCustomerBalance">
        <id column="balance_id" property="balanceId"/>
        <result column="customer_id" property="customerId"/>
        <result column="currency" property="currency"/>
        <result column="balance_amount" property="balanceAmount"/>
        <result column="frozen_amount" property="frozenAmount"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="selectByCustomerIdAndCurrency" resultMap="BaseResultMap">
        SELECT balance_id, customer_id, currency, balance_amount, frozen_amount, updated_at
        FROM fx_customer_balance
        WHERE customer_id = #{customerId} AND currency = #{currency}
    </select>

    <select id="selectByCustomerId" resultMap="BaseResultMap">
        SELECT balance_id, customer_id, currency, balance_amount, frozen_amount, updated_at
        FROM fx_customer_balance
        WHERE customer_id = #{customerId}
    </select>

    <insert id="insert" parameterType="com.xfunds.entity.FxCustomerBalance" useGeneratedKeys="true" keyProperty="balanceId">
        INSERT INTO fx_customer_balance (customer_id, currency, balance_amount, frozen_amount, updated_at)
        VALUES (#{customerId}, #{currency}, #{balanceAmount}, #{frozenAmount}, NOW())
    </insert>

    <update id="update" parameterType="com.xfunds.entity.FxCustomerBalance">
        UPDATE fx_customer_balance
        SET balance_amount = #{balanceAmount},
            frozen_amount = #{frozenAmount},
            updated_at = NOW()
        WHERE balance_id = #{balanceId}
    </update>

</mapper>
