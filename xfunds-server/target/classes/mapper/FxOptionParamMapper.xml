<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xfunds.mapper.FxOptionParamMapper">

    <select id="selectAll" resultType="com.xfunds.entity.FxOptionParam">
        SELECT param_id, param_code, param_name, param_value, description, updated_at
        FROM fx_option_param
        ORDER BY param_code
    </select>

    <select id="selectByParamCode" resultType="com.xfunds.entity.FxOptionParam">
        SELECT param_id, param_code, param_name, param_value, description, updated_at
        FROM fx_option_param
        WHERE param_code = #{paramCode}
    </select>

    <!-- 更新期权参数：按参数编码更新参数名称、参数值与描述 -->
    <update id="update" parameterType="com.xfunds.entity.FxOptionParam">
        UPDATE fx_option_param
        <set>
            <if test="paramName != null">param_name = #{paramName},</if>
            <if test="paramValue != null">param_value = #{paramValue},</if>
            <if test="description != null">description = #{description},</if>
        </set>
        WHERE param_code = #{paramCode}
    </update>

</mapper>
